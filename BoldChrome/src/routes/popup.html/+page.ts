// Legacy compatibility shim
// The SvelteKit page loader lives in `+page.ts`. This file exists to keep tooling
// or scripts that expect `page.ts` from failing.

// No exports needed; see `+page.ts` for loader logic.
export {};
